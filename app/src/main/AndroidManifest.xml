<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="kzeedesigns.peerkamil1.MainActivity" />
        <activity android:name="kzeedesigns.peerkamil1.HomeActivity" />
        <activity android:name="kzeedesigns.peerkamil1.StartActivity" />

        <activity
            android:name="kzeedesigns.peerkamil1.Welcome"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="kzeedesigns.peerkamil1.AboutActivity" />
        <activity android:name="kzeedesigns.peerkamil1.scndhalf"/>
    </application>

</manifest>